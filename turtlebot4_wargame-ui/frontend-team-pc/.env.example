# 실제 rosbridge_server / 백엔드로 교체할 때는 이 두 줄만 바꾸면 됩니다.
# mock 백엔드(:8000/ws) 대신 실제 로봇에 연결된 ros-humble-rosbridge-server(기본 포트 9090)를 바라보도록 변경.
# (ros2 launch rosbridge_server rosbridge_websocket.launch.xml 로 띄운 뒤 사용)
VITE_ROSBRIDGE_URL=ws://localhost:9090
VITE_API_URL=http://localhost:8000
